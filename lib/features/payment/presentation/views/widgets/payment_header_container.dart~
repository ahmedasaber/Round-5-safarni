import 'package:flutter/material.dart';
import 'package:safarni/core/utils/app_styles.dart';
import 'package:safarni/features/payment/presentation/views/widgets/payment_card_internal_widgets.dart';

import '../../../../../core/utils/app_assets.dart';
import '../../../../../core/utils/app_colors.dart';
class PaymentHeaderContainer extends StatelessWidget {
  const PaymentHeaderContainer({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      padding: const EdgeInsets.all(TextStyles.defaultSpace - 4),
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(TextStyles.borderRadiusLg + 4),
        gradient: const LinearGradient(
          colors: [AppColors.primaryCardColor, AppColors.secondaryCardColor],
          begin: Alignment.bottomLeft,
          end: Alignment.bottomRight,
        ),
        boxShadow: [
          BoxShadow(
            color: Colors.black12,
            blurRadius: 10,
            offset: Offset(0, 6),
          ),
        ],
      ),
      // the blue card
      child: PaymentCardInternalWidgets(),
    );
  }
}


